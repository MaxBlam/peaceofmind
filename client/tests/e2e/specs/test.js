// https://docs.cypress.io/api/introduction/api.html

describe('FOLDER_CREATE_NONAME_NOGRADE_NOTEACHER', () => {
  it('Creates a new Folder without a name, grade and teacher', () => {
    expect(true).to.equal(true);
  });
});
